[pytest]
# Add the src/ directory to sys.path so tests can `import calculator`, etc.
pythonpath = src

# Default options applied to every run.
addopts = -ra -q

testpaths = tests

# Register custom markers (avoids "unknown marker" warnings).
markers =
    smoke: quick sanity-check tests
    slow: tests that take a while to run
